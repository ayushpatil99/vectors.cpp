#include<iostream>
#include<vector>
using namespace std;

/* In this program we are going to study about most commonly used functions in vector:

1) .size()-> used to find the size of the vector
2) .push_back(value) -> used to insert values in the vector
3) .pop_back() -> used to pop the last value in the vector
4) .front() -> used to get the front value ina vector
5) .back() -> used to get the last/back value in the vector
6) .at()-> similar use as vec[0] can also be written as vec.at(0)

*/